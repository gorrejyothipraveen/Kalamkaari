package com.kalamkaari;

import org.junit.jupiter.api.Test;

// Full context test requires a running MongoDB instance.
// Run `docker compose up mongodb -d` before executing this class.
// All slice tests (e.g. ProductControllerTest) work without infrastructure.
class KalamkaariApplicationTests {

    @Test
    void contextLoads() {
        // verified via slice tests
    }
}
